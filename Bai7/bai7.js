const a = 5;
const b = 10;
console.log("Fifteen is " + (a + b) + " and\nnot " + (2 * a + b) + ".");
console.log('Cách 2 sử dụng template string thay vì cộng chuỗi')
console.log(`Fifteen is ${a + b} and
not ${2 * a + b}.`);
console.log(`in ra gia tri cua a = ${a}, b = ${b}, va a + b = ${a+b}`)